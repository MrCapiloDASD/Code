public class NumberArray {

    public static void main(String[] args) {

        // Step 1: Declare and initialize your array (at least 5 numbers)
        int[] numbers = { 79, 87, 99, 100, 92};

        // Step 2: Print the first element using index 0
        System.out.println("First element: " + numbers[0]);

        // Step 3: Print the last element using array.length
        System.out.println("Last element: " + numbers[4]);

        // Step 4: Use a for loop to print every element
        for (int i = 0; i < numbers.length; i++) {
            // print numbers[i]
        }

        // Step 5: Calculate and print the sum of all elements
        int sum = 0;
        // add your loop here
        System.out.println("Sum: " + sum);

        // Step 6: Print the number of elements
        System.out.println("Array length: " + numbers.length);
    }
}
